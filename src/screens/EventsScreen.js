import React from 'react';
import ContentPageScreen from './ContentPageScreen';

export default function EventsScreen() {
  return <ContentPageScreen title="Events" subtitle="Calendar and schedules (prototype)." />;
}
